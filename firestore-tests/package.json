{
  "name": "littlelamb-rules-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Firestore + Storage security-rules unit tests (run against the emulator).",
  "scripts": {
    "test": "firebase --config ../firebase.json --project=littlelamb-sb-staging emulators:exec --only firestore,storage \"vitest run\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^15.20.0",
    "vitest": "^1.6.1"
  }
}
